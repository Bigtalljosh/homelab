## Global Settings (shared — keep in sync with ../media/.env)
DOCKERCONFDIR=/volume1/docker

# IMPORTANT (UGREEN migration): SSH into the UGREEN and run `id <your-user>` to get the
# correct values, then chown config files to match (see ../MIGRATION.md).
PUID=1026
PGID=100
TZ=Europe/London
