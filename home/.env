## Global Settings (shared — keep in sync with ../media/.env)
DOCKERCONFDIR=/volume2/docker

# UGREEN UGOS: bigtalljosh = uid 1000; gid 100 = shared `users` group. See ../MIGRATION.md.
PUID=1000
PGID=100
TZ=Europe/London
